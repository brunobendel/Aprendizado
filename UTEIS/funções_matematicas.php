<?php

    //Funções matematicas
    echo pi();
    $numeros = [1,10,5000,-20,15]; //tbm pode ser feito assim: = array(x,y)
    echo min($numeros); // mostra 5000
    echo max($numeros); // mostra -20

    // abs(x);  -> converte o x para absoluto ex: -20 para 20

    // sqrt(y); -> raiz quadrada de y

    //$z = 5.5;
    //roud(z);  -> arredondamento, arredonda para 6 nesse caso.

    //echo rand(0,500);

?>